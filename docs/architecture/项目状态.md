# 当前项目状态

> 这是动态事实的唯一维护位置。更新日期：2026-08-02。

## 当前规模

- 数据库表：13 张
- 注册工具：16 个
- 业务技能：4 个
- 后端自动化测试：207 个通过，1 条依赖库弃用警告

测试结果来自：

```bash
.venv/bin/pytest -q
```

## 已实现

- 订单、商品、物流、退款和人工升级主链路；
- 规则快路与模型意图兜底；
- 缺信息续接和退款二次确认；
- 技能级工具白名单与危险写工具拒绝；
- 用户身份、订单和工单归属校验；
- 消息幂等、退款多层幂等和会话级去重；
- 智能体会话精确消息绑定和原子抢占；
- 工单版本比较更新和业务副作用同事务回滚；
- FastAPI、PostgreSQL、Redis、Celery 和本地线程模式；
- 用户与管理员认证、角色权限和人工处理接口；
- 服务端事件流、轮询降级和前端管理看板；
- 链路追踪、工具审计、脱敏、令牌成本、服务时限和监控指标；
- 确定性会话质检、离线评测和真实模型评测入口；
- PostgreSQL 并发验证、Redis/Celery 联调和接入层压力测试归档。

## 当前明确边界

- 退款只创建内部申请，没有接入支付渠道；
- 登录方案是项目级密码哈希与签名令牌，不等同企业统一身份平台；
- 数据库提交与消息投递之间尚未使用事务事件表；
- 服务端事件流推送任务进度，不是逐字模型输出；
- 禁语扫描是基础护栏，不是完整内容安全系统；
- 单进程模型熔断状态不在多个工作进程之间共享；
- 接入层压力测试不能代表真实模型吞吐；
- 模拟模型评测不能代表真实语义泛化。

## 交付前验证

```bash
.venv/bin/pytest -q
python -m scripts.demo_recruitment
python -m app.eval.run_eval
npm --prefix frontend run build
docker compose -f deploy/docker-compose.yml config -q
```

完整环境还应验证：

- PostgreSQL 并发幂等；
- Redis/Celery 重投与会话抢占；
- 登录、两轮退款、进度推送和管理员处理闭环；
- 压力测试结果与队列深度归档。
